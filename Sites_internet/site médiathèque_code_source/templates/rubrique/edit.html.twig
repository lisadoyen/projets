{% extends "layout.html.twig" %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block body %}
    <div class="container cadre">

        <h1 class="titre-responsive">Modifier la Rubrique</h1>

        <div class="text-show-responsive-detail">
            {{ include('rubrique/_form.html.twig', {'button_label': 'Modifier'}) }}
        </div>

        <div class="text-show-responsive-detail">
            <div class="row">
                <div class="col">{{ include('rubrique/_delete_form.html.twig') }}</div>
            </div>
        </div>

        <a href="{{ path('rubrique_index') }}" class="edit-btn btn-responsive-global ml-0">Liste des rubriques</a>
        <a href="{{ path('crud_list') }}" class="edit-btn btn-responsive-global ml-0">Menu</a>

    </div>
{% endblock %}
