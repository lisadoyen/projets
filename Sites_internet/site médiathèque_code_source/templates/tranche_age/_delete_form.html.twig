<form method="post" action="{{ path('tranche_age_delete', {'id': tranche_age.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tranche_age.id) }}">
    <button class="edit-btn btn-responsive-global">Supprimer</button>
</form>
